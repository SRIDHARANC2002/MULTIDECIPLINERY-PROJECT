import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="main-header">
            <h1 className="header-title">Smart Navigation System</h1>
        </header>
    );
};

export default Header;
