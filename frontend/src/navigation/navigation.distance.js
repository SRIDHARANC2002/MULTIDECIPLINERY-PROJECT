const calculateDistance = (coord1, coord2) => {
    // Placeholder for distance calculation logic (e.g., Haversine formula)
    return Math.random() * 1000; // Random distance for demo
};

export default {
    calculateDistance
};
